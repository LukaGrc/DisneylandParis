{% extends 'base.html.twig' %}

{% block title %}{{ restaurant.name }} à Disneyland Paris{% endblock %}

{% block body %}

<div class="page-title">
    <h1>{{ restaurant.name }}</h1>
    <p>{{ restaurant.description }}</p>
</div>

{% endblock %}
